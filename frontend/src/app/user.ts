export class User {
    search: string = "";
    select: string = "Artist";

    constructor (
        search : string,
        select : string
    )  {
        this.search = search;
        this.select = select;
    }



    
}
